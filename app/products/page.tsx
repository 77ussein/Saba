"use client";
import{useState}from"react";import{Search,SlidersHorizontal}from"lucide-react";import{products}from"@/lib/data";import{ProductCard}from"@/components/product-card";
export default function Products(){const[q,setQ]=useState("");const list=products.filter(p=>p.name.includes(q)||p.nameEn.toLowerCase().includes(q.toLowerCase()));return <div className="mx-auto max-w-7xl px-6 py-16 lg:px-8"><div className="mb-12 flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="text-xs tracking-[.25em] text-primary">مجموعة SABA</p><h1 className="mt-3 text-4xl font-bold md:text-6xl">كل المنتجات</h1><p className="mt-3 text-sm text-muted-foreground">{list.length} قطعة مختارة بعناية</p></div><label className="flex min-w-72 items-center gap-3 border-b py-3"><Search/><span className="sr-only">بحث</span><input value={q} onChange={e=>setQ(e.target.value)} className="w-full bg-transparent outline-none" placeholder="ابحث في المجموعة..."/></label></div><div className="mb-8 flex items-center justify-between border-y py-4"><button className="flex items-center gap-2 text-sm"><SlidersHorizontal/> تصفية وفرز</button><div className="flex gap-5 text-xs"><button>الكل</button><button>العطور</button><button>المجوهرات</button><button>الهدايا</button></div></div><div className="grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">{list.map(p=><ProductCard key={p.slug} p={p}/>)}</div></div>}
