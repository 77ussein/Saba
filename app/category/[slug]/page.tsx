import{products}from"@/lib/data";import{ProductCard}from"@/components/product-card";
export default async function Category({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const names:Record<string,string>={perfumes:"العطور",jewelry:"المجوهرات",gifts:"الهدايا"};const name=names[slug]||"مجموعة SABA";const list=products.filter(p=>p.category===name);return <div><div className="pattern bg-secondary px-6 py-20 text-center text-secondary-foreground"><p className="text-xs tracking-[.25em] text-primary">عالم SABA</p><h1 className="mt-4 text-5xl font-bold">{name}</h1><p className="mx-auto mt-5 max-w-xl text-sm leading-7 text-background/70">قطع مختارة تمزج أصالة الحرفة اليمنية مع حسّ التصميم المعاصر.</p></div><div className="mx-auto grid max-w-7xl grid-cols-2 gap-4 px-6 py-16 md:grid-cols-3 lg:grid-cols-4">{(list.length?list:products.slice(0,4)).map(p=><ProductCard key={p.slug} p={p}/>)}</div></div>}
