"use client";
import Image from"next/image";import Link from"next/link";import{Heart,ShoppingBag,Star}from"lucide-react";import type{Product}from"@/lib/data";import{useStore}from"./store-provider";
export function ProductCard({p}:{p:Product}){const{add,wish}=useStore();const discount=Math.round((1-p.price/p.original)*100);return <article className="group relative"><div className="relative aspect-[4/5] overflow-hidden bg-muted"><Image src={p.image} alt={p.name} fill className="object-cover transition duration-700 group-hover:scale-105"/><span className="absolute start-3 top-3 bg-foreground px-3 py-1 text-[10px] text-background">-{discount}%</span><button onClick={()=>wish(p)} className="absolute end-3 top-3 grid size-10 place-items-center rounded-full bg-background/90 transition hover:bg-primary" aria-label="أضف للمفضلة"><Heart/></button><button onClick={()=>add(p)} className="absolute inset-x-4 bottom-4 flex translate-y-3 items-center justify-center gap-2 bg-foreground px-4 py-3 text-xs text-background opacity-0 transition group-hover:translate-y-0 group-hover:opacity-100"><ShoppingBag/>إضافة للسلة</button></div><div className="flex flex-col gap-2 py-4"><div className="flex items-center gap-1 text-xs text-primary"><Star fill="currentColor"/> {p.rating} <span className="text-muted-foreground">({p.reviews})</span></div><Link href={`/product/${p.slug}`} className="text-base font-semibold hover:text-primary">{p.name}</Link><div className="flex items-center gap-3"><b>{p.price} ر.س</b><del className="text-sm text-muted-foreground">{p.original} ر.س</del></div></div></article>}
